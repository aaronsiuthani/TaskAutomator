# MACRO RECORDER

A Python script to record keyboard macros and play them back.

# Installation
'pip install -r requirements.txt'

# Usage
Run the script and choose
- `r` to record a macro
- `p` to play a recorded macro
Recording is saved to `macro.json`.

Press 'esc' to stop recording
